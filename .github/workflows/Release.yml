name: Release CMakeMM

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Release CMakeMM
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1

      - name: Set GetCMakeMM version by tag
        run: |
          mkdir dist
          mv Entry.cmake dist/Entry.cmake
          mv CMakeMM.cmake dist/CMakeMM.cmake
      #    sed "s/CMAKEMM_VERSION/${GITHUB_REF/refs\/tags\/v}/g" GetCMakeMM.cmake > dist/GetCMakeMM.cmake

      - name: Upload CMakeMM to release
        uses: svenstaro/upload-release-action@v1-release
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          tag: ${{ github.ref }}
          file: dist/*.cmake
          file_glob: true
          overwrite: true
