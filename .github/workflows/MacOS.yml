name: MacOS

on:
  push:
    branches:
    - main
  pull_request:
    branches:
    - main

jobs:
  tests:
    name: '🧪 Test'
    runs-on: macos-latest

    steps:
    - name: '🧰 Checkout'
      uses: actions/checkout@v2.3.4
      with:
        fetch-depth: 0

    - name: '🧪 Run tests'
      run: |
        cmake -DBUILD_TESTS=ON -S . -B build
