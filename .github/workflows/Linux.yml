name: Linux

on:
  push:
    branches:
    - main
  pull_request:
    branches:
    - main

jobs:
  test:
    name: '🧪 Test'
    runs-on: ubuntu-latest

    steps:
    - name: '🧰 Checkout'
      uses: actions/checkout@v2.3.4
      with:
        fetch-depth: 0

    - name: '🧪 Run tests'
      run: |
        cmake -DBUILD_TESTS=ON -S . -B build
